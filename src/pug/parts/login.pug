.login.active
    form
        .input-group
            input#email.input-area(type='email' required='true')
            label.label(for='email') Электронная почта
        .input-group.login-bot
            input#pass.input-area(type='password' required='true')
            label.label(for='pass') Пароль
        .login-forget
            span Забыли пароль
        button(type='submit' disabled) Войти
        .login-registration
            span Регистрация